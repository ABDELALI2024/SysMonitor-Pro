# SysMonitor Pro - General settings

# Directories
LOG_DIR="${SCRIPT_DIR:-$(pwd)}/logs"
REPORT_DIR="${SCRIPT_DIR:-$(pwd)}/reports"

# Services to monitor (space-separated systemd units)
CRITICAL_SERVICES=(ssh cron rsyslog NetworkManager firewalld)

# Specific process to monitor (name or PID). Leave empty to skip
PROCESS_TO_MONITOR=""

# Network connectivity target
PING_TARGET="8.8.8.8"


